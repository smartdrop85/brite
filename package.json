{
  "name": "brite",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run --spec cypress/e2e/**/* -b chrome",
    "test-firefox": "npx cypress run --spec cypress/e2e/**/* -b firefox --env grepTags=firefox",
    "test-api": "npx cypress run --spec cypress/api/**/*"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cucumber": "^6.0.7",
    "cypress": "^13.17.0",
    "cypress-grep": "^2.6.1",
    "dayjs": "^1.11.13"
  }
}
